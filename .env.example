### Example environment variables for backend and client
# Backend (server): use service role key only on server
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=service-role-REPLACE_ME

# Client (Flutter): use anon/public key in the app (do NOT store the service role key here)
SUPABASE_ANON_KEY=anon-REPLACE_ME

# Optional: backend port for uvicorn
PORT=8000
